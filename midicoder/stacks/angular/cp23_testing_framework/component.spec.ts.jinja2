/*
 * Unit tests cho {{ entity.get('id', 'Entity') }} component.
 * Tự động sinh bởi CP23 — Testing Framework Generator.
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { {{ entity.get('id', 'Entity') if entity else 'Entity' }}Component } from './{{ entity.get('id', 'Entity').lower() if entity else 'entity' }}-component';

describe('{{ entity.get('id', 'Entity') if entity else 'Entity' }}Component', () => {
  let component: {{ entity.get('id', 'Entity') if entity else 'Entity' }}Component;
  let fixture: ComponentFixture<{{ entity.get('id', 'Entity') if entity else 'Entity' }}Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [{{ entity.get('id', 'Entity') if entity else 'Entity' }}Component],
    }).compileComponents();

    fixture = TestBed.createComponent({{ entity.get('id', 'Entity') if entity else 'Entity' }}Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('nên tạo component', () => {
    expect(component).toBeTruthy();
  });
});
