/*
 * MFA Service — {{ 'Full MFA' if rule_count > 1 else 'Basic MFA' }} Configuration.
 *
 * Generated by Midicoder CE — CP46 MFA & Advanced Authentication.
 * Do NOT edit manually.
 */

import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

export interface MfaEnrollRequest {
  userId: string;
  method: string;
  deviceName?: string;
  phoneNumber?: string;
  issuer?: string;
}

export interface MfaChallengeRequest {
  userId: string;
  method: string;
}

export interface MfaVerifyRequest {
  sessionId: string;
  otpCode?: string;
  signature?: string;
}

@Injectable({
  providedIn: 'root',
})
export class MfaService {
  private apiUrl = '/api/mfa';

  constructor(private http: HttpClient) {}

  enroll(request: MfaEnrollRequest): Observable<any> {
    return this.http.post(`${this.apiUrl}/enroll`, request);
  }

  verifyEnrollment(enrollmentId: string, verificationCode: string): Observable<any> {
    return this.http.post(`${this.apiUrl}/enroll/verify`, {
      enrollmentId,
      verificationCode,
    });
  }

  createChallenge(userId: string, method: string): Observable<any> {
    return this.http.post(`${this.apiUrl}/challenge`, { userId, method });
  }

  verifyChallenge(sessionId: string, otpCode: string, signature?: string): Observable<any> {
    return this.http.post(`${this.apiUrl}/verify`, {
      sessionId,
      otpCode,
      signature,
    });
  }

  getStatus(userId: string): Observable<any> {
    return this.http.get(`${this.apiUrl}/status/${userId}`);
  }

  disable(credentialId: string): Observable<any> {
    return this.http.post(`${this.apiUrl}/disable/${credentialId}`, {});
  }

  revoke(credentialId: string): Observable<any> {
    return this.http.post(`${this.apiUrl}/revoke/${credentialId}`, {});
  }
}
