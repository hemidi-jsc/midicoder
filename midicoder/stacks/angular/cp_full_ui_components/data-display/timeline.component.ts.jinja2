{% if ui_framework == "material" %}import { Component, OnInit, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MatIconModule } from '@angular/material/icon';

@Component({
  selector: '{{ selector }}',
  standalone: true,
  imports: [CommonModule, MatIconModule],
  templateUrl: './{{ template_file_name }}.html',
  styles: []
})
export class {{ component_class_name }}Component implements OnInit {
  @Input() events: {
    title: string;
    date: string | Date;
    description?: string;
    status?: 'in-progress' | 'completed' | 'pending';
    icon?: string;
  }[] = [];
  @Input() mode: 'standard' | 'dense' = 'standard';
  @Input() hideBackdrop: boolean = false;

  ngOnInit(): void {}

  getStatusColor(status?: string): string {
    switch (status) {
      case 'in-progress': return '#2196f3';
      case 'completed': return '#4caf50';
      case 'pending': return '#ff9800';
      default: return '#757575';
    }
  }

  formatDate(date: string | Date): string {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
}
{% elif ui_framework == "tailwind" %}import { Component, OnInit, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: '{{ selector }}',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './{{ template_file_name }}.html',
  styles: []
})
export class {{ component_class_name }}Component implements OnInit {
  @Input() events: {
    title: string;
    date: string | Date;
    description?: string;
    status?: 'in-progress' | 'completed' | 'pending';
    icon?: string;
  }[] = [];
  @Input() variant: 'default' | 'alternate' = 'default';
  @Input() direction: 'left' | 'right' = 'left';
  @Input() hideDates: boolean = false;

  ngOnInit(): void {}

  getStatusClasses(status?: string): string {
    switch (status) {
      case 'in-progress': return 'bg-blue-500 ring-blue-100';
      case 'completed': return 'bg-green-500 ring-green-100';
      case 'pending': return 'bg-yellow-500 ring-yellow-100';
      default: return 'bg-gray-500 ring-gray-100';
    }
  }

  formatDate(date: string | Date): string {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
}
{% elif ui_framework == "bootstrap" %}import { Component, OnInit, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { NgBootstrapModule } from 'ng-bootstrap';

@Component({
  selector: '{{ selector }}',
  standalone: true,
  imports: [CommonModule, NgBootstrapModule],
  templateUrl: './{{ template_file_name }}.html',
  styles: []
})
export class {{ component_class_name }}Component implements OnInit {
  @Input() events: {
    title: string;
    date: string | Date;
    description?: string;
    status?: 'in-progress' | 'completed' | 'pending';
    icon?: string;
  }[] = [];
  @Input() alternate: boolean = false;

  ngOnInit(): void {}

  getStatusBadge(status?: string): string {
    switch (status) {
      case 'in-progress': return 'bg-primary';
      case 'completed': return 'bg-success';
      case 'pending': return 'bg-warning';
      default: return 'bg-secondary';
    }
  }

  formatDate(date: string | Date): string {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
}
{% elif ui_framework == "antd" %}import { Component, OnInit, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: '{{ selector }}',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './{{ template_file_name }}.html',
  styles: []
})
export class {{ component_class_name }}Component implements OnInit {
  @Input() events: {
    title: string;
    date: string | Date;
    description?: string;
    status?: 'in-progress' | 'finished' | 'error' | 'default';
    icon?: string;
  }[] = [];
  @Input() pending: boolean = false;
  @Input() reverse: boolean = false;
  @Input() mode: 'default' | 'clear' = 'default';

  ngOnInit(): void {}

  formatDate(date: string | Date): string {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
}
{% elif ui_framework == "carbon" %}import { Component, OnInit, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: '{{ selector }}',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './{{ template_file_name }}.html',
  styles: []
})
export class {{ component_class_name }}Component implements OnInit {
  @Input() events: {
    title: string;
    date: string | Date;
    description?: string;
    status?: 'in-progress' | 'completed' | 'pending';
    icon?: string;
  }[] = [];
  @Input() size: 'sm' | 'md' = 'md';

  ngOnInit(): void {}

  getStatusColor(status?: string): string {
    switch (status) {
      case 'in-progress': return '#0f62fe';
      case 'completed': return '#198038';
      case 'pending': return '#d69e2e';
      default: return '#8d8d8d';
    }
  }

  formatDate(date: string | Date): string {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
}
{% else %}import { Component, OnInit, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: '{{ selector }}',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './{{ template_file_name }}.html',
  styles: []
})
export class {{ component_class_name }}Component implements OnInit {
  @Input() events: {
    title: string;
    date: string | Date;
    description?: string;
  }[] = [];

  ngOnInit(): void {}

  formatDate(date: string | Date): string {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
}
{% endif %}
