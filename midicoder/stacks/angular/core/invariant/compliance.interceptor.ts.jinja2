/**
 * HTTP compliance interceptor cho Invariant Enforcement.
 * Auto-generated bởi Midicoder CP52 - Không sửa tay.
 */

import {
  Injectable,
  HttpRequest,
  HttpResponse,
  HttpHandler,
  HttpEvent,
} from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable()
export class ComplianceInterceptorInterceptor {
  /**
   * Intercept HTTP requests để enforce compliance.
   */
  intercept(
    request: HttpRequest<any>,
    next: HttpHandler
  ): Observable<HttpEvent<any>> {
    // Compliance check sẽ được inject bởi CP52 runtime
    return next.handle(request);
  }
}
