/** Auto-generated Angular routes config. */

import { Routes } from "@angular/router";

{% macro render_route(route, indent_level) %}
{{ '  ' * indent_level }}{
{{ '  ' * indent_level }}  path: '{{ route.path }}',
{{ '  ' * indent_level }}  loadComponent: () => import('./components/{{ route.component|lower }}/{{ route.component|lower }}.component.ts') then(m => m.default)
{% if route.children %}
{{ '  ' * indent_level }}  children: [
{% for child in route.children %}
{{ render_route(child, indent_level + 2) }}
{% endfor %}
{{ '  ' * indent_level }}  ],
{% endif %}
{{ '  ' * indent_level }}},
{% endmacro %}

const routes: Routes = [
{% for route in routes %}
{{ render_route(route, 1) }}
{% endfor %}
];

export default routes;
