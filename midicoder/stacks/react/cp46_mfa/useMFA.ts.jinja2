/*
 * useMFA Hook — {{ 'Full MFA' if rule_count > 1 else 'Basic MFA' }} Configuration.
 *
 * Generated by Midicoder CE — CP46 MFA & Advanced Authentication.
 * Do NOT edit manually.
 */

import { useState, useCallback } from 'react';
import { mfaApi } from '../../api/mfa-api';
import { MFAMethod } from '../../types/mfa-types';

export function useMFA() {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const enroll = useCallback(async (
    userId: string,
    method: MFAMethod,
    deviceName?: string,
    phoneNumber?: string,
  ) => {
    setLoading(true);
    setError(null);
    try {
      const result = await mfaApi.enroll({ userId, method, deviceName, phoneNumber });
      return result;
    } catch (err: any) {
      setError(err.message || 'Enrollment failed');
      return null;
    } finally {
      setLoading(false);
    }
  }, []);

  const createChallenge = useCallback(async (userId: string, method: MFAMethod) => {
    setLoading(true);
    setError(null);
    try {
      const result = await mfaApi.createChallenge(userId, method);
      return result;
    } catch (err: any) {
      setError(err.message || 'Challenge creation failed');
      return null;
    } finally {
      setLoading(false);
    }
  }, []);

  const verifyChallenge = useCallback(async (sessionId: string, otpCode: string, signature?: string) => {
    setLoading(true);
    setError(null);
    try {
      const result = await mfaApi.verifyChallenge(sessionId, otpCode, signature);
      return result;
    } catch (err: any) {
      setError(err.message || 'Verification failed');
      return null;
    } finally {
      setLoading(false);
    }
  }, []);

  const getStatus = useCallback(async (userId: string) => {
    setLoading(true);
    setError(null);
    try {
      const result = await mfaApi.getStatus(userId);
      return result;
    } catch (err: any) {
      setError(err.message || 'Failed to get status');
      return null;
    } finally {
      setLoading(false);
    }
  }, []);

  return {
    enroll,
    createChallenge,
    verifyChallenge,
    getStatus,
    loading,
    error,
  };
}

export default useMFA;
