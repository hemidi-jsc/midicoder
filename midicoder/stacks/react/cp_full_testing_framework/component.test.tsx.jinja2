/**
 * Unit tests cho {{ entity.get('id', 'Entity') }} component.
 * Tự động sinh bởi CP23 — Testing Framework Generator.
 */
import { render, screen } from '@testing-library/react';
import { {{ entity.get('id', 'Entity') if entity else 'Entity' }} } from '../{{ entity.get('id', 'Entity').lower() if entity else 'entity' }}';

describe('{{ entity.get('id', 'Entity') if entity else 'Entity' }}', () => {
  it('nên render component', () => {
    render(<{{ entity.get('id', 'Entity') if entity else 'Entity' }} />);
    expect(screen.getByRole('main')).toBeInTheDocument();
  });
});
