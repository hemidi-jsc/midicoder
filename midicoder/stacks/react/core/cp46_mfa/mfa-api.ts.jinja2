/*
 * MFA API — {{ 'Full MFA' if rule_count > 1 else 'Basic MFA' }} Configuration.
 *
 * Generated by Midicoder CE — CP46 MFA & Advanced Authentication.
 * Do NOT edit manually.
 */

import { MFAMethod, MFAEnrollment, MFAChallenge, MFAVerifyResponse, MFAStatus } from '../types/mfa-types';

const API_BASE = '/api/mfa';

interface EnrollRequest {
  userId: string;
  method: MFAMethod;
  deviceName?: string;
  phoneNumber?: string;
  issuer?: string;
}

export const mfaApi = {
  async enroll(request: EnrollRequest): Promise<MFAEnrollment> {
    const response = await fetch(`${API_BASE}/enroll`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(request),
    });
    if (!response.ok) {
      throw new Error('Enrollment failed');
    }
    return response.json();
  },

  async verifyEnrollment(enrollmentId: string, verificationCode: string): Promise<any> {
    const response = await fetch(`${API_BASE}/enroll/verify`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ enrollmentId, verificationCode }),
    });
    if (!response.ok) {
      throw new Error('Verification failed');
    }
    return response.json();
  },

  async createChallenge(userId: string, method: MFAMethod): Promise<MFAChallenge> {
    const response = await fetch(`${API_BASE}/challenge`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ userId, method }),
    });
    if (!response.ok) {
      throw new Error('Challenge creation failed');
    }
    return response.json();
  },

  async verifyChallenge(sessionId: string, otpCode: string, signature?: string): Promise<MFAVerifyResponse> {
    const response = await fetch(`${API_BASE}/verify`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ sessionId, otpCode, signature }),
    });
    if (!response.ok) {
      throw new Error('Verification failed');
    }
    return response.json();
  },

  async getStatus(userId: string): Promise<MFAStatus> {
    const response = await fetch(`${API_BASE}/status/${userId}`);
    if (!response.ok) {
      throw new Error('Failed to get status');
    }
    return response.json();
  },

  async disable(credentialId: string): Promise<any> {
    const response = await fetch(`${API_BASE}/disable/${credentialId}`, {
      method: 'POST',
    });
    if (!response.ok) {
      throw new Error('Disable failed');
    }
    return response.json();
  },

  async revoke(credentialId: string): Promise<any> {
    const response = await fetch(`${API_BASE}/revoke/${credentialId}`, {
      method: 'POST',
    });
    if (!response.ok) {
      throw new Error('Revoke failed');
    }
    return response.json();
  },
};

export default mfaApi;
