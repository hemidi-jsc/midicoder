{% if ui_framework == "material" %}import React, { Component } from 'react';
import {
  Card,
  CardContent,
  CardActions,
  Typography,
  Button,
  Box,
  IconButton,
} from '@mui/material';
import RefreshIcon from '@mui/icons-material/Refresh';
import ErrorOutlineIcon from '@mui/icons-material/ErrorOutline';
import type { SxProps, Theme } from '@mui/material/styles';

interface ErrorBoundaryProps {
  title?: string;
  message?: string;
  showRetry?: boolean;
  retryText?: string;
  maxWidth?: number;
  sx?: SxProps<Theme>;
  className?: string;
  onRetry?: () => void;
  children?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  handleRetry = (): void => {
    this.setState({ hasError: false, error: null });
    if (this.props.onRetry) {
      this.props.onRetry();
    }
  };

  render(): React.ReactNode {
    if (this.state.hasError) {
      return (
        <Card
          sx={% raw %}{{
            maxWidth: this.props.maxWidth || 600,
            mx: 'auto',
            my: 4,
            textAlign: 'center',
            p: 3,
            ...this.props.sx
          }}{% endraw %}
          className={this.props.className}
        >
          <CardContent>
            <ErrorOutlineIcon color="error" sx=% raw %}{{ fontSize: 48, mb: 2 }}{% endraw %} />
            <Typography variant="h5" component="h2" gutterBottom>
              {this.props.title || 'Something went wrong'}
            </Typography>
            <Typography variant="body2" color="text.secondary" paragraph>
              {this.props.message || 'An unexpected error has occurred'}
            </Typography>
            {this.props.children}
          </CardContent>
          {this.props.showRetry !== false && (
            <CardActions sx=% raw %}{{ justifyContent: 'center' }}{% endraw %}>
              <Button variant="contained" color="primary" startIcon={<RefreshIcon />} onClick={this.handleRetry}>
                {this.props.retryText || 'Try again'}
              </Button>
            </CardActions>
          )}
        </Card>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
{% elif ui_framework == "tailwind" %}import React, { Component } from 'react';

interface ErrorBoundaryProps {
  title?: string;
  message?: string;
  showRetry?: boolean;
  retryText?: string;
  className?: string;
  onRetry?: () => void;
  children?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  handleRetry = (): void => {
    this.setState({ hasError: false, error: null });
    if (this.props.onRetry) {
      this.props.onRetry();
    }
  };

  render(): React.ReactNode {
    if (this.state.hasError) {
      return (
        <div className={`max-w-md mx-auto p-6 bg-white rounded-lg shadow-md text-center ${this.props.className || ''}`}>
          <div className="text-red-500 text-5xl mb-4">&#9888;</div>
          <h2 className="text-xl font-semibold text-gray-900 mb-2">
            {this.props.title || 'Something went wrong'}
          </h2>
          <p className="text-gray-600 mb-4">
            {this.props.message || 'An unexpected error has occurred'}
          </p>
          {this.props.children}
          {this.props.showRetry !== false && (
            <button
              onClick={this.handleRetry}
              className="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
            >
              {this.props.retryText || 'Try again'}
            </button>
          )}
        </div>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
{% elif ui_framework == "bootstrap" %}import React, { Component } from 'react';

interface ErrorBoundaryProps {
  title?: string;
  message?: string;
  showRetry?: boolean;
  retryText?: string;
  className?: string;
  onRetry?: () => void;
  children?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  handleRetry = (): void => {
    this.setState({ hasError: false, error: null });
    if (this.props.onRetry) {
      this.props.onRetry();
    }
  };

  render(): React.ReactNode {
    if (this.state.hasError) {
      return (
        <div className={`card mx-auto text-center ${this.props.className || ''}`} style=% raw %}{{ maxWidth: 600 }}{% endraw %}>
          <div className="card-body p-4">
            <div className="text-danger display-4 mb-3">&#9888;</div>
            <h2 className="card-title h5">
              {this.props.title || 'Something went wrong'}
            </h2>
            <p className="card-text text-muted">
              {this.props.message || 'An unexpected error has occurred'}
            </p>
            {this.props.children}
            {this.props.showRetry !== false && (
              <button className="btn btn-primary mt-3" onClick={this.handleRetry}>
                {this.props.retryText || 'Try again'}
              </button>
            )}
          </div>
        </div>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
{% elif ui_framework == "antd" %}import React, { Component } from 'react';
import { Alert, Button, Result } from 'antd';

interface ErrorBoundaryProps {
  title?: string;
  message?: string;
  showRetry?: boolean;
  retryText?: string;
  className?: string;
  onRetry?: () => void;
  children?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  handleRetry = (): void => {
    this.setState({ hasError: false, error: null });
    if (this.props.onRetry) {
      this.props.onRetry();
    }
  };

  render(): React.ReactNode {
    if (this.state.hasError) {
      return (
        <Result
          status="error"
          title={this.props.title || 'Something went wrong'}
          subTitle={this.props.message || 'An unexpected error has occurred'}
          extra={this.props.showRetry !== false ? (
            <Button type="primary" onClick={this.handleRetry}>
              {this.props.retryText || 'Try again'}
            </Button>
          ) : undefined}
        >
          {this.props.children}
        </Result>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
{% elif ui_framework == "carbon" %}import React, { Component } from 'react';

interface ErrorBoundaryProps {
  title?: string;
  message?: string;
  showRetry?: boolean;
  retryText?: string;
  className?: string;
  onRetry?: () => void;
  children?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  handleRetry = (): void => {
    this.setState({ hasError: false, error: null });
    if (this.props.onRetry) {
      this.props.onRetry();
    }
  };

  render(): React.ReactNode {
    if (this.state.hasError) {
      return (
        <div className=% raw %}`cds--card mx-auto text-center bg-white border border-gray-200 rounded p-6 ${this.props.className || ''}`{% endraw %}>
          <div className="text-red-600 text-4xl mb-3">&#9888;</div>
          <h2 className="cds--title text-lg font-semibold mb-2">
            {this.props.title || 'Something went wrong'}
          </h2>
          <p className="cds--label text-gray-600 mb-4">
            {this.props.message || 'An unexpected error has occurred'}
          </p>
          {this.props.children}
          {this.props.showRetry !== false && (
            <button
              className="cds-btn cds-btn--primary"
              onClick={this.handleRetry}
            >
              {this.props.retryText || 'Try again'}
            </button>
          )}
        </div>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
{% else %}import React, { Component } from 'react';

interface ErrorBoundaryProps {
  title?: string;
  message?: string;
  showRetry?: boolean;
  retryText?: string;
  className?: string;
  onRetry?: () => void;
  children?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  handleRetry = (): void => {
    this.setState({ hasError: false, error: null });
    if (this.props.onRetry) {
      this.props.onRetry();
    }
  };

  render(): React.ReactNode {
    if (this.state.hasError) {
      return (
        <div className={`border rounded p-4 text-center mx-auto max-w-md ${this.props.className || ''}`}>
          <div className="text-red-500 text-4xl mb-3">&#9888;</div>
          <h2 className="font-semibold mb-2">
            {this.props.title || 'Something went wrong'}
          </h2>
          <p className="text-gray-600 mb-4">
            {this.props.message || 'An unexpected error has occurred'}
          </p>
          {this.props.children}
          {this.props.showRetry !== false && (
            <button className="px-4 py-2 bg-blue-600 text-white rounded" onClick={this.handleRetry}>
              {this.props.retryText || 'Try again'}
            </button>
          )}
        </div>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
{% endif %}
