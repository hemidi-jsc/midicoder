/** Register Page - CP21.
*
* Trang đăng ký tài khoản mới.
*
* UI Framework: [{ui_framework}]
*/

import React, { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
{% if ui_framework == 'antd' %}
import { Form, Input, Button, Card, Typography, Alert, Divider } from "antd";
{% endif %}

/** Register Page Props */
export interface RegisterPageProps {
redirectPath?: string;
}

/** Register Page Component. */
export const Register: React.FC<RegisterPageProps> = ({
    redirectPath = "/login",
    }) => {
    const navigate = useNavigate();
    const [loading, setLoading] = useState(false);
    const [error, setError] = useState<string | null>(null);
        const [success, setSuccess] = useState(false);

        const handleSubmit = async (values: { email: string; password: string; confirmPassword: string; fullName: string
        }) => {
        if (values.password !== values.confirmPassword) {
        setError("Mật khẩu xác nhận không khớp.");
        return;
        }
        setLoading(true);
        setError(null);
        try {
        // TODO: gọi API đăng ký
        setSuccess(true);
        setTimeout(() => navigate(redirectPath), 2000);
        } catch (err: any) {
        setError(err.message || "Đăng ký thất bại.");
        } finally {
        setLoading(false);
        }
        };

        {% if ui_framework == 'antd' %}
        const [form] = Form.useForm();
        const fullFormStyle = { minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center",
        background: "#f0f2f5" };
        const cardStyle = { width: 400 };
        const alertStyle = { marginBottom: 16 };
        const mtStyle = { marginTop: 16, textAlign: "center" };
        const nameRule = { required: true };
        const emailRule = { required: true, type: "email" };
        const passRule = { required: true, min: 8 };

        if (success) {
        return (
        <div style={fullFormStyle}>
            <Card style={cardStyle} title={<Typography.Title level={3}>Đăng ký thành công</Typography.Title>}>
                <Alert message="Đăng ký thành công! Đang chuyển đến trang đăng nhập..." type="success"
                    style={alertStyle} />
            </Card>
        </div>
        );
        }

        return (
        <div style={fullFormStyle}>
            <Card style={cardStyle} title={<Typography.Title level={3}>Đăng ký tài khoản</Typography.Title>}>
                {error &&
                <Alert message={error} type="error" style={alertStyle} />}
                <Form form={form} onFinish={handleSubmit}>
                    <Form.Item name="fullName" rules={[nameRule]}>
                        <Input size="large" placeholder="Họ và tên" />
                    </Form.Item>
                    <Form.Item name="email" rules={[emailRule]}>
                        <Input size="large" placeholder="Email" />
                    </Form.Item>
                    <Form.Item name="password" rules={[passRule]}>
                        <Input.Password size="large" placeholder="Mật khẩu" />
                    </Form.Item>
                    <Form.Item name="confirmPassword" rules={[nameRule]}>
                        <Input.Password size="large" placeholder="Xác nhận mật khẩu" />
                    </Form.Item>
                    <Form.Item>
                        <Button type="primary" htmlType="submit" size="large" block loading={loading}>
                            Đăng ký
                        </Button>
                    </Form.Item>
                </Form>
                <Divider>
                    <Typography.Text type="secondary">hoặc</Typography.Text>
                </Divider>
                <div style={mtStyle}>
                    <Link to="/login">Đã có tài khoản? Đăng nhập</Link>
                </div>
            </Card>
        </div>
        );
        {% else %}
        const pageStyle = { minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center" };
        const cardStyle = { maxWidth: 400, width: "100%", padding: 24, border: "1px solid #ddd", borderRadius: 8 };
        const titleStyle = { textAlign: "center" };
        const errStyle = { color: "red", marginBottom: 12 };
        const okStyle = { color: "green", marginBottom: 12 };
        const inputStyle = { width: "100%", padding: 8, marginBottom: 12 };
        const btnStyle = { width: "100%", padding: 12, marginTop: 8 };
        const linkStyle = { marginTop: 12, textAlign: "center" };
        const emptyValues = { email: "", password: "", confirmPassword: "", fullName: "" };

        if (success) {
        return (
        <div style={pageStyle}>
            <div style={cardStyle}>
                <h2 style={titleStyle}>Đăng ký thành công</h2>
                <p style={okStyle}>Đang chuyển đến trang đăng nhập...</p>
            </div>
        </div>
        );
        }

        return (
        <div style={pageStyle}>
            <div style={cardStyle}>
                <h2 style={titleStyle}>Đăng ký tài khoản</h2>
                {error && <div style={errStyle}>{error}</div>}
                <form onSubmit={(e)=> { e.preventDefault(); handleSubmit(emptyValues); }}>
                    <input type="text" placeholder="Họ và tên" required style={inputStyle} />
                    <input type="email" placeholder="Email" required style={inputStyle} />
                    <input type="password" placeholder="Mật khẩu" required style={inputStyle} />
                    <input type="password" placeholder="Xác nhận mật khẩu" required style={inputStyle} />
                    <button type="submit" disabled={loading} style={btnStyle}>
                        {loading ? "Đang đăng ký..." : "Đăng ký"}
                    </button>
                </form>
                <div style={linkStyle}>
                    <Link to="/login">Đã có tài khoản? Đăng nhập</Link>
                </div>
            </div>
        </div>
        );
        {% endif %}
        };

        export default Register;