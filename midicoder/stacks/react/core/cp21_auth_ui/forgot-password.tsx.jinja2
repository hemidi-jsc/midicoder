/** Forgot Password Page - CP21.
*
* Trang yêu cầu reset mật khẩu — nhập email để nhận link.
*
* UI Framework: [{ui_framework}]
*/

import React, { useState } from "react";
import { Link } from "react-router-dom";
{% set fw = ui_framework | default("tailwind") %}
{% if fw == "material" %}
import { Container, Paper, TextField, Button, Typography, Box, Alert } from "@mui/material";
{% elif fw == "tailwind" %}
{# Tailwind uses utility classes, no UI library import #}
{% elif fw == "bootstrap" %}
import { Container, Card, Form, Button, Alert } from "react-bootstrap";
{% elif fw == "antd" %}
import { Form, Input, Button, Card, Typography, Alert } from "antd";
{% elif fw == "carbon" %}
import { Form, FormGroup, TextInput, Button, InlineNotification, Grid, Column, Layer } from "@carbon/react";
{% endif %}

const { Title, Text } = Typography;

export interface ForgotPasswordPageProps {}

export const ForgotPassword: React.FC<ForgotPasswordPageProps> = () => {
    const [loading, setLoading] = useState(false);
    const [error, setError] = useState<string | null>(null);
        const [emailSent, setEmailSent] = useState(false);

        const handleSubmit = async (values: { email: string }) => {
        setLoading(true);
        setError(null);
        try {
        // TODO: gọi API forgot password
        setEmailSent(true);
        } catch (err: any) {
        setError(err.message || "Không thể gửi email.");
        } finally {
        setLoading(false);
        }
        };

        {% if fw == "antd" %}
        const [form] = Form.useForm();
        const fullFormStyle = { minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center",
        background: "#f0f2f5" };
        const cardStyle = { width: 400 };
        const alertStyle = { marginBottom: 16 };
        const mtStyle = { marginTop: 16, textAlign: "center" };
        const emailRule = { required: true, type: "email" };

        if (emailSent) {
        return (
        <div style={fullFormStyle}>
            <Card style={cardStyle} title={<Typography.Title level={3}>Email đã gửi</Typography.Title>}>
                <Alert message="Chúng tôi đã gửi link reset mật khẩu đến email của bạn." type="success"
                    style={alertStyle} />
                <div style={mtStyle}>
                    <Link to="/login">Quay lại đăng nhập</Link>
                </div>
            </Card>
        </div>
        );
        }

        return (
        <div style={fullFormStyle}>
            <Card style={cardStyle} title={<Typography.Title level={3}>Quên mật khẩu</Typography.Title>}>
                {error &&
                <Alert message={error} type="error" style={alertStyle} />}
                <Form form={form} onFinish={handleSubmit}>
                    <Form.Item name="email" rules={[emailRule]}>
                        <Input size="large" placeholder="Email" />
                    </Form.Item>
                    <Form.Item>
                        <Button type="primary" htmlType="submit" size="large" block loading={loading}>
                            Gửi link reset
                        </Button>
                    </Form.Item>
                </Form>
                <div style={mtStyle}>
                    <Link to="/login">Quay lại đăng nhập</Link>
                </div>
            </Card>
        </div>
        );
        {% elif fw == "material" %}
        const loginStyle = { mt: 8 };
        const paperStyle = { p: 4 };
        const btnStyle = { mt: 2, mb: 1 };
        const linkStyle = { mt: 2, textAlign: "center" };
        const emptyValues = { email: "" };

        if (emailSent) {
        return (
        <Container maxWidth="sm" sx={loginStyle}>
            <Paper elevation={3} sx={paperStyle}>
                <Typography variant="h4" align="center" gutterBottom>Email đã gửi</Typography>
                <Alert severity="success" sx={{ mb: 2 }}>
                    Chúng tôi đã gửi link reset mật khẩu đến email của bạn.
                </Alert>
                <Box sx={linkStyle}>
                    <Link to="/login">Quay lại đăng nhập</Link>
                </Box>
            </Paper>
        </Container>
        );
        }

        return (
        <Container maxWidth="sm" sx={loginStyle}>
            <Paper elevation={3} sx={paperStyle}>
                <Typography variant="h4" align="center" gutterBottom>Quên mật khẩu</Typography>
                {error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}
                <Box component="form" onSubmit={(e)=> { e.preventDefault(); handleSubmit(emptyValues); }}>
                    <TextField fullWidth label="Email" type="email" margin="normal" required />
                    <Button fullWidth variant="contained" type="submit" sx={btnStyle} disabled={loading}>
                        {loading ? "Đang gửi..." : "Gửi link reset"}
                    </Button>
                </Box>
                <Box sx={linkStyle}>
                    <Link to="/login">Quay lại đăng nhập</Link>
                </Box>
            </Paper>
        </Container>
        );
        {% elif fw == "tailwind" %}
        const emptyValues = { email: "" };

        if (emailSent) {
        return (
        <div className="min-h-screen flex items-center justify-center bg-gray-50">
            <div className="max-w-md w-full space-y-8 p-8 bg-white rounded-lg shadow-md">
                <h2 className="text-3xl font-bold text-center">Email đã gửi</h2>
                <div className="bg-green-100 text-green-700 p-3 rounded">
                    Chúng tôi đã gửi link reset mật khẩu đến email của bạn.
                </div>
                <div className="text-center">
                    <Link to="/login" className="text-blue-600">Quay lại đăng nhập</Link>
                </div>
            </div>
        </div>
        );
        }

        return (
        <div className="min-h-screen flex items-center justify-center bg-gray-50">
            <div className="max-w-md w-full space-y-8 p-8 bg-white rounded-lg shadow-md">
                <h2 className="text-3xl font-bold text-center">Quên mật khẩu</h2>
                {error && <div className="bg-red-100 text-red-700 p-3 rounded">{error}</div>}
                <form onSubmit={(e)=> { e.preventDefault(); handleSubmit(emptyValues); }} className="space-y-4">
                    <input type="email" placeholder="Email" required className="w-full p-3 border rounded" />
                    <button type="submit" disabled={loading}
                        className="w-full p-3 bg-blue-600 text-white rounded hover:bg-blue-700">
                        {loading ? "Đang gửi..." : "Gửi link reset"}
                    </button>
                </form>
                <div className="text-center">
                    <Link to="/login" className="text-blue-600">Quay lại đăng nhập</Link>
                </div>
            </div>
        </div>
        );
        {% elif fw == "bootstrap" %}
        const emptyValues = { email: "" };

        if (emailSent) {
        return (
        <Container className="d-flex align-items-center justify-content-center" style={{ minHeight: "100vh" }}>
            <Card style={{ maxWidth: 400, width: "100%" }}>
                <Card.Body>
                    <h2 className="text-center">Email đã gửi</h2>
                    <Alert variant="success">Chúng tôi đã gửi link reset mật khẩu đến email của bạn.</Alert>
                    <div className="text-center mt-3">
                        <Link to="/login">Quay lại đăng nhập</Link>
                    </div>
                </Card.Body>
            </Card>
        </Container>
        );
        }

        return (
        <Container className="d-flex align-items-center justify-content-center" style={{ minHeight: "100vh" }}>
            <Card style={{ maxWidth: 400, width: "100%" }}>
                <Card.Body>
                    <h2 className="text-center">Quên mật khẩu</h2>
                    {error && <Alert variant="danger">{error}</Alert>}
                    <Form onSubmit={(e)=> { e.preventDefault(); handleSubmit(emptyValues); }}>
                        <Form.Group className="mb-3">
                            <Form.Control type="email" placeholder="Email" required />
                        </Form.Group>
                        <Button variant="primary" type="submit" disabled={loading} className="w-100">
                            {loading ? "Đang gửi..." : "Gửi link reset"}
                        </Button>
                    </Form>
                    <div className="text-center mt-3">
                        <Link to="/login">Quay lại đăng nhập</Link>
                    </div>
                </Card.Body>
            </Card>
        </Container>
        );
        {% elif fw == "carbon" %}
        const emptyValues = { email: "" };

        if (emailSent) {
        return (
        <div style={{ minHeight: "100vh" , display: "flex" , alignItems: "center" , justifyContent: "center" }}>
            <Grid condense>
                <Column>
                    <Layer>
                        <div className="cds--content" style={{ maxWidth: 400, width: "100%" }}>
                            <h2 className="cds--text-truncate">Email đã gửi</h2>
                            <InlineNotification kind="success" title="Thành công" lowContrast>
                                Chúng tôi đã gửi link reset mật khẩu đến email của bạn.
                            </InlineNotification>
                            <div className="cds--list" style={{ marginTop: 16, textAlign: "center" }}>
                                <Link to="/login">Quay lại đăng nhập</Link>
                            </div>
                        </div>
                    </Layer>
                </Column>
            </Grid>
        </div>
        );
        }

        return (
        <div style={{ minHeight: "100vh" , display: "flex" , alignItems: "center" , justifyContent: "center" }}>
            <Grid condense>
                <Column>
                    <Layer>
                        <div className="cds--content" style={{ maxWidth: 400, width: "100%" }}>
                            <h2 className="cds--text-truncate">Quên mật khẩu</h2>
                            {error && (
                            <InlineNotification kind="error" title="Lỗi" lowContrast onClose={()=> setError(null)}>
                                {error}
                            </InlineNotification>
                            )}
                            <Form onSubmit={(e)=> { e.preventDefault(); handleSubmit(emptyValues); }}>
                                <FormGroup>
                                    <TextInput id="email" label="Email" type="email" placeholder="Email" required />
                                </FormGroup>
                                <Button kind="primary" type="submit" disabled={loading} style={{ width: "100%" }}>
                                    {loading ? "Đang gửi..." : "Gửi link reset"}
                                </Button>
                            </Form>
                            <div className="cds--list" style={{ marginTop: 16, textAlign: "center" }}>
                                <Link to="/login">Quay lại đăng nhập</Link>
                            </div>
                        </div>
                    </Layer>
                </Column>
            </Grid>
        </div>
        );
        {% endif %}
        };

        export default ForgotPassword;