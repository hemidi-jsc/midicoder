// Storybook preview config — Auto-generated bởi Midicoder CE (CP26)
// KHÔNG chỉnh sửa thủ công

/** @type { import('@storybook/react').Preview } */
const preview = {
  parameters: {
    actions: { argTypesRegex: '^on[A-Z].*' },
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/,
      },
    },
  },
};

export default preview;
