# variables.tf
# Terraform Variables for {{ config.app_name }}
# Generated by Midicoder - DO NOT EDIT MANUALLY

# ============================================================================
# General Configuration
# ============================================================================

variable "app_name" {
  description = "Name of the application"
  type        = string
  default     = "{{ config.app_name }}"
}

variable "environment" {
  description = "Environment name (development, staging, production)"
  type        = string
  default     = "{{ config.environment }}"
}

variable "aws_region" {
  description = "AWS region to deploy resources"
  type        = string
  default     = "{{ config.region }}"
}

# ============================================================================
# ECS Configuration
# ============================================================================

variable "ecs_cpu" {
  description = "CPU units for ECS tasks"
  type        = number
  default     = {{ config.ecs_cpu }}
}

variable "ecs_memory" {
  description = "Memory in MB for ECS tasks"
  type        = number
  default     = {{ config.ecs_memory }}
}

# ============================================================================
# RDS Configuration
# ============================================================================

variable "db_engine" {
  description = "Database engine type"
  type        = string
  default     = "{{ config.db_engine }}"
}

variable "db_version" {
  description = "Database engine version"
  type        = string
  default     = "{{ config.db_version }}"
}

variable "db_instance_class" {
  description = "RDS instance class"
  type        = string
  default     = "{{ config.db_instance_class }}"
}

variable "db_allocated_storage" {
  description = "Allocated storage in GB"
  type        = number
  default     = {{ config.db_allocated_storage }}
}

variable "postgres_password" {
  description = "PostgreSQL master username password"
  type        = string
  sensitive   = true
  
  # Generate random password if not provided
  # In production, use AWS Secrets Manager or pass via -var flag
}

# ============================================================================
# Redis Configuration
# ============================================================================

variable "redis_node_type" {
  description = "ElastiCache cache node type"
  type        = string
  default     = "{{ config.redis_node_type }}"
}

variable "redis_num_nodes" {
  description = "Number of cache nodes"
  type        = number
  default     = {{ config.redis_num_nodes }}
}

# ============================================================================
# S3 Configuration
# ============================================================================

variable "s3_bucket_prefix" {
  description = "Prefix for S3 bucket names"
  type        = string
  default     = "{{ config.s3_bucket_prefix }}"
}

# ============================================================================
# Secrets
# ============================================================================

variable "neo4j_password" {
  description = "Neo4j database password"
  type        = string
  sensitive   = true
}

variable "jwt_secret" {
  description = "JWT secret key for authentication"
  type        = string
  sensitive   = true
}