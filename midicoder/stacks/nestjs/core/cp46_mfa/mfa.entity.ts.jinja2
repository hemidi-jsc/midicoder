/*
 * MFA Entity — {{ 'Full MFA' if rule_count > 1 else 'Basic MFA' }} Configuration.
 *
 * Generated by Midicoder CE — CP46 MFA & Advanced Authentication.
 * Do NOT edit manually.
 */

import {
  Column,
  CreateDateColumn,
  Entity,
  PrimaryGeneratedColumn,
  UpdateDateColumn,
} from 'typeorm';

export enum MFAMethodEntity {
  TOTP = 'totp',
  SMS_OTP = 'sms_otp',
  WEBAUTHN_FIDO2 = 'webauthn_fido2',
  BIOMETRIC = 'biometric',
}

export enum MFAMethodStatusEntity {
  ENABLED = 'enabled',
  DISABLED = 'disabled',
  PENDING_VERIFICATION = 'pending_verification',
  REVOKED = 'revoked',
}

export enum MFAPriorityEntity {
  REQUIRED = 'required',
  OPTIONAL = 'optional',
  BACKUP = 'backup',
}

@Entity('mfa_credentials')
export class MfaCredentialEntity {
  @PrimaryGeneratedColumn('uuid')
  credentialId: string;

  @Column({ name: 'user_id' })
  userId: string;

  @Column({
    type: 'enum',
    enum: MFAMethodEntity,
    default: MFAMethodEntity.TOTP,
  })
  method: MFAMethodEntity;

  @Column({
    type: 'enum',
    enum: MFAMethodStatusEntity,
    default: MFAMethodStatusEntity.PENDING_VERIFICATION,
  })
  status: MFAMethodStatusEntity;

  @Column({
    type: 'enum',
    enum: MFAPriorityEntity,
    default: MFAPriorityEntity.REQUIRED,
  })
  priority: MFAPriorityEntity;

  @Column({ nullable: true })
  secret: string;

  @Column({ name: 'public_key', nullable: true })
  publicKey: string;

  @Column({ name: 'phone_number', nullable: true })
  phoneNumber: string;

  @Column({ name: 'device_name', nullable: true })
  deviceName: string;

  @Column({ name: 'last_used_at', nullable: true })
  lastUsedAt: Date;

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date;

  @UpdateDateColumn({ name: 'updated_at' })
  updatedAt: Date;
}
