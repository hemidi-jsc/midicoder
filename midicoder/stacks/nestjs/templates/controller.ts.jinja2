/**
 * {{ entity['id'] }} Controller.
 * 
 * Generated by Midicoder - BackendNestJSEmitter
 * CP06: API Gateway & Service Mesh
 * KPI-029: Tenant Isolation - Multi-tenant support
 */

import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  HttpCode,
  HttpStatus,
} from '@nestjs/common';

import { {{ entity['id'] }}Service } from './{{ entity['id'] | lower }}.service';
import { Create{{ entity['id'] }}Dto } from './dto/create.{{ entity['id'] | lower }}.dto';
import { Update{{ entity['id'] }}Dto } from './dto/update.{{ entity['id'] | lower }}.dto';
// KPI-029: Tenant Isolation - Import @CurrentTenant decorator
import { CurrentTenant } from './tenant/tenant.decorators';

@Controller('{{ entity['id'] | lower }}s')
export class {{ entity['id'] }}Controller {
  /**
   * {{ entity.get('description', entity['id']) }} Controller.
   * 
   * KPI-029: Tenant Isolation - Controller nhận tenantId từ request context.
   * 
   * @param service - {{ entity['id'] }}Service instance
   */
  constructor(private readonly {{ entity['id'] | lower }}Service: {{ entity['id'] }}Service) {}

  /**
   * Tạo mới {{ entity.get('description', entity['id'].lower()) }}.
   * 
   * KPI-029: Tenant Isolation - tenantId được tự động inject từ request context.
   * 
   * @param tenantId - Tenant ID từ request context
   * @param createDto - Create{{ entity['id'] }}Dto
   * @returns {{ entity['id'] }} entity vừa tạo
   */
  @Post()
  @HttpCode(HttpStatus.CREATED)
  create(@CurrentTenant() tenantId: string, @Body() createDto: Create{{ entity['id'] }}Dto) {
    return this.{{ entity['id'] | lower }}Service.create(tenantId, createDto);
  }

  /**
   * List tất cả {{ entity.get('description', entity['id'].lower()) }}.
   * 
   * KPI-029: Tenant Isolation - Chỉ trả về entities của tenant tương ứng.
   * 
   * @param tenantId - Tenant ID từ request context
   * @returns List {{ entity['id'] }} entities của tenant
   */
  @Get()
  findAll(@CurrentTenant() tenantId: string) {
    return this.{{ entity['id'] | lower }}Service.findAll(tenantId);
  }

  /**
   * Tìm {{ entity.get('description', entity['id'].lower()) }} theo ID.
   * 
   * KPI-029: Tenant Isolation - Kiểm tra entity thuộc tenant nào.
   * 
   * @param tenantId - Tenant ID từ request context
   * @param id - ID của {{ entity.get('description', entity['id'].lower()) }}
   * @returns {{ entity['id'] }} entity hoặc undefined
   */
  @Get(':id')
  findOne(@CurrentTenant() tenantId: string, @Param('id') id: string) {
    return this.{{ entity['id'] | lower }}Service.findOne(tenantId, id);
  }

  /**
   * Cập nhật {{ entity.get('description', entity['id'].lower()) }}.
   * 
   * KPI-029: Tenant Isolation - Chỉ update được entity của tenant tương ứng.
   * 
   * @param tenantId - Tenant ID từ request context
   * @param id - ID của {{ entity.get('description', entity['id'].lower()) }}
   * @param updateDto - Update{{ entity['id'] }}Dto
   * @returns {{ entity['id'] }} entity đã cập nhật
   */
  @Patch(':id')
  update(
    @CurrentTenant() tenantId: string,
    @Param('id') id: string,
    @Body() updateDto: Update{{ entity['id'] }}Dto,
  ) {
    return this.{{ entity['id'] | lower }}Service.update(tenantId, id, updateDto);
  }

  /**
   * Xóa {{ entity.get('description', entity['id'].lower()) }}.
   * 
   * KPI-029: Tenant Isolation - Chỉ xóa được entity của tenant tương ứng.
   * 
   * @param tenantId - Tenant ID từ request context
   * @param id - ID của {{ entity.get('description', entity['id'].lower()) }}
   * @returns Result của xóa
   */
  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  remove(@CurrentTenant() tenantId: string, @Param('id') id: string) {
    return this.{{ entity['id'] | lower }}Service.remove(tenantId, id);
  }
}
