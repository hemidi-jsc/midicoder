/**
 * CouponCode Value Object
 * 
 * MÃ£ mÃ£ giáº£m giÃ¡.
 * Generated by Midicoder - BackendNestJSEmitter
 * CP01: Domain Model - Value Objects
 * \r\n * KPI-029: Tenant Isolation - Value Objects are tenant-scoped when used in tenant contexts\r\n * 
 * Tags: domain, marketing, dp05
 */

export interface CouponCodeData {
  code: string;
  normalized?: string;
}

export class CouponCode {
  readonly code: string;

  private constructor(code: string) {
    this.code = code;
  }

  /**
   * Táº¡o CouponCode vá»›i validation.
   */
  static create(code: string): CouponCode {
    if (!code || !code.trim()) {
      throw new Error('MÃ£ giáº£m giÃ¡ khÃ´ng Ä‘Æ°á»£c Ä‘á»ƒ trá»‘ng');
    }

    if (!/^[A-Z0-9-]{4,30}$/.test(code.toUpperCase())) {
      throw new Error('MÃ£ giáº£m giÃ¡ chá»‰ chá»©a chá»¯ hoa, sá»‘, gáº¡ch ngang (4-30 kÃ½ tá»±)');
    }

    return new CouponCode(code);
  }

  /**
   * Tráº£ vá» code chuáº©n hÃ³a (uppercase).
   */
  get normalized(): string {
    return this.code.toUpperCase();
  }

  /**
   * Chuyá»ƒn thÃ nh object.
   */
  toJSON(): CouponCodeData {
    return {
      code: this.code,
      normalized: this.normalized,
    };
  }

  /**
   * Táº¡o CouponCode tá»« object.
   */
  static fromJSON(data: CouponCodeData): CouponCode {
    return new CouponCode(data.code || data.normalized || '');
  }

  /**
   * String representation.
   */
  toString(): string {
    return this.normalized;
  }

  /**
   * So sÃ¡nh báº±ng (case-insensitive).
   */
  equals(other: CouponCode): boolean {
    return this.code.toUpperCase() === other.code.toUpperCase();
  }

  /**
   * Hash code.
   */
  hashCode(): number {
    return this.normalized.charCodeAt(0);
  }
}
