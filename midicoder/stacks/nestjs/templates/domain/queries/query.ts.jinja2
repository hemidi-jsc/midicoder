/**
 * {{ query_id }} Query Definition.
 *
 * Query class định nghĩa input parameters cho {{ query_id }} query.
 *
 * Generated by Midicoder - DO NOT EDIT MANUALLY
 */

import { IsOptional, IsInt, Min, IsString, IsArray, IsObject, Max } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class {{ query_id }} {
  /**
   * Query cho {{ query_description|default('lấy dữ liệu') }}.
   */

{% for input_param in query_input %}
  @ApiProperty({
    required: {{ 'true' if input_param.required|default(false) else 'false' }},
    description: '{{ input_param.description|default(input_param.type) }}',
    type: {{ input_param.type }},
    default: {{ input_param.default|default('undefined') }},
  })
  @IsOptional()
  {{ input_param.name }}: {{ input_param.type }} = {{ input_param.default|default('undefined') }};
{% endfor %}

  // Pagination parameters
  @ApiProperty({
    required: false,
    description: 'Page number (1-indexed)',
    type: Number,
    minimum: 1,
    default: 1,
  })
  @IsOptional()
  @IsInt()
  @Min(1)
  page: number = 1;

  @ApiProperty({
    required: false,
    description: 'Items per page (max 100)',
    type: Number,
    minimum: 1,
    maximum: 100,
    default: 20,
  })
  @IsOptional()
  @IsInt()
  @Min(1)
  @Max(100)
  page_size: number = 20;

  @ApiProperty({
    required: false,
    description: 'Cursor for cursor-based pagination',
    type: String,
  })
  @IsOptional()
  @IsString()
  cursor?: string;

  // Projection parameters
  @ApiProperty({
    required: false,
    description: 'Fields to include',
    type: [String],
  })
  @IsOptional()
  @IsArray()
  fields?: string[];

  @ApiProperty({
    required: false,
    description: 'Fields to exclude',
    type: [String],
  })
  @IsOptional()
  @IsArray()
  exclude_fields?: string[];

  // Filter parameters (GraphQL style)
  @ApiProperty({
    required: false,
    description: 'Filter conditions (GraphQL style)',
    type: Object,
  })
  @IsOptional()
  @IsObject()
  filters: Record<string, any> = {};

  // Sort parameters
  @ApiProperty({
    required: false,
    description: 'Field to sort by',
    type: String,
  })
  @IsOptional()
  @IsString()
  sort_by?: string;

  @ApiProperty({
    required: false,
    description: 'Sort order (asc or desc)',
    type: String,
    enum: ['asc', 'desc'],
    default: 'asc',
  })
  @IsOptional()
  @IsString()
  sort_order: 'asc' | 'desc' = 'asc';

  /**
   * Chuyển query sang object.
   *
   * @returns Dictionary representation của query
   */
  toObject(): Record<string, any> {
    return {
      query_id: '{{ query_id }}',
{% for input_param in query_input %}
      {{ input_param.name }}: this.{{ input_param.name }},
{% endfor %}
      pagination: {
        type: this.cursor ? 'cursor' : 'offset',
        page: this.page,
        page_size: this.page_size,
        cursor: this.cursor,
      },
      projection: {
        include: this.fields,
        exclude: this.exclude_fields || ['password', 'secret_key', 'token'],
      },
      filters: this.filters,
      sort: {
        by: this.sort_by,
        order: this.sort_order,
      },
    };
  }

  /**
   * Lấy tenant_id từ query context.
   *
   * @returns Tenant ID nếu có, undefined nếu global query
   */
  getTenantId(): string | undefined {
    // Tenant ID sẽ được inject từ request context
    return undefined;
  }

  toString(): string {
    return `{{ query_id }}{ page: ${this.page}, page_size: ${this.page_size}, filters: ${JSON.stringify(this.filters)} }`;
  }
}