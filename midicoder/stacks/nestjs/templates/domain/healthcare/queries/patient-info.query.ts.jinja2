/**
 * Patient Info Query (NestJS/TypeScript)
 *
 * Truy vấn thông tin bệnh nhân với PHI masking (RX04).
 */

import { IsString, IsNotEmpty } from 'class-validator';

export class PatientInfoInput {
  @IsString()
  @IsNotEmpty()
  patientId: string;
}

export class PatientInfoOutput {
  patientId: string;
  maskedName: string;
  gender: string;
  bloodType: string;
  status: string;
}

export class PatientInfoQuery {
  name = 'healthcare.patient_info';
  description = 'Truy vấn thông tin bệnh nhân với PHI masking';
  guards = ['tenant_scope', 'auth_required', 'permission:healthcare:patient:read'];
  effects = ['read_record', 'phi_masking', 'write_audit_log'];

  async execute(input: PatientInfoInput, context: any): Promise<PatientInfoOutput> {
    // RX04: PHI masking for authorized users
    return {} as PatientInfoOutput;
  }
}