/**
 * Application Configuration.
 * 
 * Generated by Midicoder - BackendNestJSEmitter
 * CP01: Domain Model
 * KPI-029: Tenant Isolation - Multi-tenant configuration
 */

export const config = {
  /**
   * Application name.
   */
  appName: process.env.APP_NAME || 'Midicoder API',
  
  /**
   * Server port.
   */
  port: parseInt(process.env.PORT, 10) || 3000,
  
  /**
   * Environment (development, production).
   */
  environment: process.env.NODE_ENV || 'development',
  
  /**
   * Database URL.
   */
  databaseUrl: process.env.DATABASE_URL || 'postgresql://localhost:5432/app',
  
  /**
   * JWT secret key.
   */
  jwtSecret: process.env.JWT_SECRET || 'change-me-in-production',
  
  /**
   * JWT expiration time.
   */
  jwtExpiration: process.env.JWT_EXPIRATION || '24h',
  
  /**
   * Redis URL.
   */
  redisUrl: process.env.REDIS_URL || 'redis://localhost:6379',
  
  /**
   * Enable debug mode.
   */
  debug: process.env.DEBUG === 'true',
  
  // ============================================================================
  // KPI-029: Tenant Isolation Configuration
  // ============================================================================
  
  /**
   * Tenant isolation strategy - KPI-029.
   * 
   * Các chiến lược:
   * - 'row': Cùng database, dùng tenantId column (Row-Level Security)
   * - 'schema': Cùng database, dùng schema riêng cho mỗi tenant
   * - 'database': Database riêng cho mỗi tenant (highest isolation)
   * 
   * Default: 'row' (phù hợp cho đa số use cases)
   */
  tenantStrategy: (process.env.TENANT_STRATEGY as 'row' | 'schema' | 'database') || 'row',
  
  /**
   * Tenant header name - KPI-029.
   * 
   * HTTP header name để nhận tenantId từ client request.
   * Default: 'X-Tenant-ID'
   * 
   * Usage: Client gửi header "X-Tenant-ID: tenant-uuid" trong mỗi request
   */
  tenantHeaderName: process.env.TENANT_HEADER_NAME || 'X-Tenant-ID',
  
  /**
   * Enable tenant isolation - KPI-029.
   * 
   * Bật/tắt tenant isolation toàn hệ thống.
   * Default: true
   * 
   * Warning: Không nên tắt trong production!
   */
  enableTenantIsolation: process.env.ENABLE_TENANT_ISOLATION !== 'false',
  
  /**
   * Enable subdomain tenant extraction - KPI-029.
   * 
   * Extract tenantId từ subdomain (e.g., tenant123.example.com).
   * Default: false
   * 
   * Usage: Set 'true' để enable subdomain-based tenant extraction
   */
  enableSubdomainTenant: process.env.ENABLE_SUBDOMAIN_TENANT === 'true',
  
  /**
   * System tenant IDs - KPI-029.
   * 
   * Danh sách tenant IDs đặc biệt không enforce tenant isolation.
   * Default: system, admin
   * 
   * Usage: Các tenant này có thể truy cập toàn bộ data (dành cho admin/system)
   */
  systemTenants: (process.env.SYSTEM_TENANTS || 'system,admin').split(','),
} as const;
