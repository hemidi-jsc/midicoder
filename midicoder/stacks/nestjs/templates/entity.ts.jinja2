/**
 * {{ entity['id'] }} Entity.
 * 
 * Generated by Midicoder - BackendNestJSEmitter
 * CP01: Domain Model
 * 
 * {{ entity.get('description', '') }}
 */

import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  UpdateDateColumn,
} from 'typeorm';

import { BaseEntity } from '../base.entity';
import { Audit } from '../decorators/audit.decorator';

@Audit()
@Entity('{{ entity['id'] | lower }}s')
export class {{ entity['id'] }}Entity extends BaseEntity {
  /**
   * Primary key.
   */
  @PrimaryGeneratedColumn('uuid')
  id: string;

{% for field in entity.get('fields', []) %}
  /**
   * {{ field.get('name', field['name']) }} field.
   */
  @Column({ type: '{{ field.get("type", "varchar") }}', nullable: true })
  {{ field['name'] }}: {{ field.get('type', 'string') }};

{% endfor %}

{% if entity.get('tenant_scope') == 'tenant_isolated' %}
  /**
   * Tenant ID for multi-tenancy.
   */
  @Column({ type: 'varchar', nullable: false })
  tenantId: string;

{% endif %}
}