# pytest configuration
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Coverage settings
{% if collection and collection.global_policy %}
env =
    COVERAGE_THRESHOLD={{ collection.global_policy.coverage_threshold }}
{% endif %}
