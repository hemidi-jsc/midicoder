"""
Application Configuration.

Generated by Midicoder - BackendFastAPIEmitter
CP01: Domain Model

Tenant Isolation Configuration (KPI-029):
- TENANT_STRATEGY: Chọn isolation strategy (row/schema/database)
- TENANT_CONTEXT_HEADER: Header name để extract tenant ID
- TENANT_DEFAULT: Default tenant cho unauthenticated requests
"""

from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    """
    Cấu hình ứng dụng.
    
    Attributes:
        app_name: Tên ứng dụng
        debug: Chế độ debug
        database_url: Database connection URL
        secret_key: Secret key cho JWT/signature
        
    Tenant Configuration (KPI-029 - Tenant Isolation):
        tenant_strategy: Isolation strategy (row/schema/database)
        tenant_context_header: HTTP header name để extract tenant ID
        tenant_default: Default tenant ID cho unauthenticated requests
    """
    
    # ========================================================================
    # Core Configuration
    # ========================================================================
    app_name: str = "Midicoder API"
    debug: bool = False
    database_url: str = "postgresql+asyncpg://localhost:5432/app"
    secret_key: str = "change-me-in-production"
    
    # ========================================================================
    # Tenant Isolation Configuration - KPI-029
    # ========================================================================
    tenant_strategy: str = "row"  # Options: "row", "schema", "database"
    tenant_context_header: str = "X-Tenant-ID"
    tenant_default: str = "default"


# Singleton settings instance
settings = Settings()
