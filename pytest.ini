[pytest]
addopts = --strict-markers --strict-config --junitxml=reports/junit.xml
testpaths = tests
markers =
    unit: fast isolated tests for functions and small modules
    integration: command and workflow tests across filesystem/artifacts
